# FireRedASR2S macOS 依赖（CPU / MPS 版本）
# 用法: python -m pip install -r requirements-macos.txt

# 核心依赖（macOS 使用普通 PyTorch，非 CUDA 版本）
torch>=2.1.0
torchaudio>=2.1.0
transformers>=4.51.3
numpy>=1.26.0

# FireRed 独立工具包（VAD 已不需要 FireRedASR2S 仓库）
fireredvad>=0.0.2

# Qwen3-TTS
qwen-tts>=0.1.1

# 音频处理
soundfile>=0.12.1
sounddevice>=0.4.6
kaldiio>=2.18.0
kaldi_native_fbank>=1.15

# 文本处理
sentencepiece>=0.1.99
cn2an>=0.5.23
textgrid

# VTube Studio API
pyvts

# 快速 ASR 通道（SenseVoiceSmall + Silero VAD）
funasr
modelscope
huggingface_hub

# 其他
peft>=0.13.2
